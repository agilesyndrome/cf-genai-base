// Compatibility entry point. The implementation is grouped by responsibility
// under ./core so consumers can import either the public core surface or a
// focused module.
export * from "./core/index.js";
